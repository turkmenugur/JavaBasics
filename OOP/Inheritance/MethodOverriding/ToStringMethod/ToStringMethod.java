package OOP.Inheritance.MethodOverriding.ToStringMethod;

public class ToStringMethod {
    /* ToString metodunun override edilmesi
     * 
     * toString() metodu nesnelerin metin karşılıklarının ekrana  yazılması için kullanılır.
     * toString() metodu Object sınıfı altında tanımlanmıştır yani her sınıf, toString() metoduna erişip bu metodu kullanabilir.
     * Ancak toString() metodu her zaman yeterli değildir.
     * Nesnelerin metin karşılığı yoksa toString() metodu, nesnenin sınıf ismini ve hafızadaki referansını geriye döndürür. (Example1'de örneği verildi)
     * 
     * Eğer bir nesneyi ekrana yazdırdığımızda Object sınıfındaki toString() metodunun çalıştırılmasını ve yukarıdaki değerlerin döndürülmesini istemiyorsak bu metodu override edip kendimize göre düzenlemeliyiz.
     * (Example2'de örneği verildi)
     */
}
