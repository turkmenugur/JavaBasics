package StringClass.CompareStringObjects;

public class CompareStringObjects {
    /* Java'da String nesnelerini karşılaştırırken diğer veri tiplerine göre değişik metodlar kullanırız. En önemlisi iki String metodunun birbirine eşit olup olmadığını kontrol ederken == operatörünü String nesneleri üzerinde kullanamayız.

     * Java'da String tipindeki iki veriyi == operatörü ile karşılaştırdığımızda nesneleri kontrol eder. Eğer veriler aynı fakat nesneler farklı ise hatalı sonuç verebilir. O yüzden Java'da String tipindeki değişkenleri özel metodlar ile karşılaştırırız.
     */
}
